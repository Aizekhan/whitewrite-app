# SESSION END — що робити в кінці сесії

## 1️⃣ Оновлюй TODO.md

```bash
# Запиши, що зроблено:
E:\WhiteWrite\handoff\TODO.md
```

**Формат:**
```markdown
## Зроблено сьогодні (2026-06-04)
- ✅ Landing page портовано 1:1 (hero-*, heroDrift, userdock)
- ✅ AuthModal портовано 1:1 (auth-tabs, Google Sign-In)
- ✅ Projects page — виправлено видалення (3s → 5s)

## В процесі
- ⏳ CreateProject Modal (50% — структура готова, CSS залишився)

## Наступні кроки
1. Завершити CreateProject Modal
2. Портувати Account page
3. Почати Книгу (BookScreen)
```

## 2️⃣ Запиши проблеми / блокери

**Якщо щось не вдалося:**
- Запиши в `TODO.md` секцію **Проблеми**
- Опиши, що саме заблоковано
- Вкажи, де шукати рішення (файл/рядок)

**Приклад:**
```markdown
## Проблеми
- ❌ Force graph (D3.js) не рендериться — потрібна допомога з `wt-graph.jsx`
  - Файл: `prototype/WhiteWrite WorldTree.html` lines 1200-1500
  - Можливо, не імпортовано D3.js або неправильні SVG refs
```

## 3️⃣ Закоміть зміни (якщо є запит)

**Тільки якщо користувач попросив:**
```bash
git add .
git commit -m "Опис змін"
git push
```

**НЕ комітити автоматично!** Чекати явного запиту користувача.

## 4️⃣ Перевір dev server

**Якщо dev server запущено:**
- Перевір, чи немає помилок у терміналі
- Якщо є помилки → запиши в `TODO.md`

## 5️⃣ Збережи контекст для наступної сесії

**Оновіть `FILE_MAP.md` (якщо потрібно):**
- Додай нові файли, які портовано
- Оновіть секцію **Порядок портування**
- Позначки: ✅ (зроблено), ⏳ (в процесі), ❌ (заблоковано)

## 6️⃣ Запиши ключові рішення

**Якщо прийнято важливе рішення:**
- Запиши в `CLAUDE.md` (секція **Рішення**)
- Або в окремий файл `handoff/DECISIONS.md`

**Приклад:**
```markdown
## Рішення (2026-06-04)
- **Видалення проєктів:** Використовуємо 5-секундний countdown (як у прототипі), а не modal підтвердження
- **Auth:** Firebase Auth + Google Sign-In (працює)
- **Firestore rules:** `allow list` додано для query з `where` clause
```

## 7️⃣ Підсумок для користувача

**Скажи користувачу:**
1. Що зроблено (список завдань)
2. Що в процесі (незавершені задачі)
3. Що наступне (1-2 найближчі кроки)
4. Чи є блокери (проблеми, які потребують втручання)

**Приклад:**
```
✅ Зроблено:
- Landing page 1:1 з прототипу
- AuthModal 1:1 з прототипу
- Projects page (5-сек видалення виправлено)

⏳ В процесі:
- CreateProject Modal (структура готова, CSS 50%)

🎯 Наступні кроки:
1. Завершити CreateProject Modal
2. Портувати Account page

❌ Блокерів немає — можна продовжувати.
```

---

**Готово!** Сесія завершена. Дякую за роботу! 🚀
